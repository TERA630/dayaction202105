package jp.terameteo.dayaction202105

/*

import android.graphics.drawable.Drawable
import androidx.databinding.BindingMethod
import androidx.databinding.BindingMethods
import android.widget.TextView
import androidx.core.content.res.ResourcesCompat
import androidx.databinding.BindingAdapter

object TextViewBinding {
    @JvmStatic
    @BindingAdapter(value = ["android:checked"])
    fun setChecked(view: TextView, checked: Boolean) {
        val theme = view.context.theme
        view.background = if (checked) {
            ResourcesCompat.getDrawable(view.resources, R.drawable.square_gold_gradient, theme)
        } else {
            ResourcesCompat.getDrawable(view.resources, R.drawable.square_silver_gradient, theme)
        }
    }
}*/
